Limit(20 )
└── Project([ $2.id, $4 ] )
    └── Sort([ $4 2, $2.id 1 ] )
        └── Algorithm(2 3 )
            └── CrossJoin()
                ├── Filter()
                │   └── NodeScan("Person" )
                └── Expand(IN "Person" )
                    └── ForeachRelationship(TO ":workAt" )
                        └── Filter()
                            └── NodeScan("Organisation" )