Project([ $4, $5 ] )
└── Sort([ $5 2, udf ] )
    └── Append()
        └── Append()
            └── Algorithm(0 2 )
                └── Limit(1 )
                    └── Filter()
                        └── Expand(OUT "Person" )
                            └── ForeachRelationship(FROM ":knows" 1 100 )
                                └── Filter()
                                    └── NodeScan("Person" )