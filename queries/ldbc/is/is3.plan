Project([ $2.id, $2.firstName, $2.lastName, $1.creationDate ] )
└── Expand(OUT "Person" )
    └── ForeachRelationship(FROM ":knows" )
        └── Filter($0.id==66 )
            └── NodeScan("Person" )
