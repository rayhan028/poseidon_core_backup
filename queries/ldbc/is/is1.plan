Project([ $0.firstName, $0.lastName, udf, $0.locationIP, $0.browserUsed, $2.id, $0.gender, $0.creationDate ] )
└── Expand(OUT "Place" )
    └── ForeachRelationship(FROM ":isLocatedIn" )
        └── Filter()
            └── NodeScan("Person" )